import <%= props.artifactDatabaseName %> from "<%= props.artifactDatabaseLocation %>"

export class <%= props.paschalName %>Repository {
  public async create(params: any): Promise<any> {
    try {
      // return await <%= props.artifactDatabaseName %>.statusFaturas.create({
      //   data: params,
      // });
    } catch (error: any) {
      throw new Error(error.message);
    }
  }

  public async getById(params: any): Promise<any> {
    try {
      // return await <%= props.artifactDatabaseName %>.statusFaturas.findFirst({
      //   where: {}
      // })
    } catch (error: any) {
      throw new Error(error.message);
    }
  }

  public async update(params: any): Promise<any> {
    try {
      // return await <%= props.artifactDatabaseName %>.update.findFirst({
      //   where: {},
      //   data: {},
      // });
    } catch (error: any) {
      throw new Error(error.message);
    }
  }

  public async delete(params: any): Promise<any> {
    try {
      // return await <%= props.artifactDatabaseName %>.statusFaturas.update({
      //   where: {},
      //   data: {},
      // });
    } catch (error: any) {
      throw new Error(error.message);
    }
  }
}
